A registration's <%= @section_label %> changed for <%= @event.name %>.

The submitted values are deliberately omitted from this email. Open the authorized staff view to review the registration:

<%= @participant_url %>
