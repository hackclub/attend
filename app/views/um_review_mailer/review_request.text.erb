Hey Leo,

<%= @participant.display_name %> has submitted their onboarding for <%= @event.name %> and declared that they're travelling as an unaccompanied minor (airline UM service). Their proof of UM booking needs review.

- Inbound flight UM: <%= @participant_event.travel_inbound&.is_unaccompanied_minor? ? "Yes" : "No" %>
- Outbound flight UM: <%= @participant_event.travel_outbound&.is_unaccompanied_minor? ? "Yes" : "No" %>
- Proof uploaded: <%= @participant_event.um_proof.attached? ? "Yes" : "No" %>
- Guardian double-confirmed: <%= @participant_event.um_guardian_confirmed_at.present? ? @participant_event.um_guardian_confirmed_at.strftime("%b %d, %Y %H:%M") : "Not yet" %>

Review UM status: <%= @review_url %>
